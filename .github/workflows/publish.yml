# name: Publish to PyPI
#
# Commented out -- not publishing to PyPI yet.
# This is an exploratory package.
#
# on:
#   push:
#     tags:
#       - "v*"
#   workflow_dispatch:
#     inputs:
#       version:
#         description: "Version to publish (e.g., 0.1.0)"
#         required: true
#
# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     environment:
#       name: pypi
#     permissions:
#       id-token: write
#     steps:
#       - uses: actions/checkout@v6
#       - name: Install uv
#         uses: astral-sh/setup-uv@v7
#       - name: Set up Python
#         run: uv python install 3.12
#       - name: Build package
#         run: uv build
#       - name: Publish to PyPI
#         uses: pypa/gh-action-pypi-publish@release/v1
